import { data } from '../data'

export default function Capabilities() {
  return (
    <section id="s05">
      <div className="wrap">
        <div className="mn">§05 / full-stack</div>
        <div className="s-rule" />
        <span className="s-label rv">Capabilities</span>
        <p className="cap-text rv">{data.capabilities}</p>
      </div>
    </section>
  )
}
